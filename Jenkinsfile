pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Build') {
            steps {
                    sh 'pwd'
                    sh 'echo "Running inside container"'
                }
            }
        }
    }
}
